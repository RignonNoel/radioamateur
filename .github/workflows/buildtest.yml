name: Buildtest

on:
  push:
  pull_request:

jobs:
  build:
    name: Buildtest
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - name: Install dependencies
        run: npm ci
      - name: Run build
        run: npm run build
